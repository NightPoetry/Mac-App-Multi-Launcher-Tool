# 快速开始 - 5 分钟搞定应用多开

> 最简单的一键多开方案，无需复杂操作

---

## 🚀 一键多开（推荐）

### 步骤 1：下载项目

克隆或下载本项目到本地：

```bash
git clone <项目地址>
cd MacAppMultiLauncher
```

### 步骤 2：执行脚本

```bash
sudo bash app-dual.sh
```

**就这么简单！** 脚本会自动完成所有配置并启动多开应用。

---

## 📝 手动操作（了解原理）

如果想了解每个步骤的原理，可以手动执行以下 4 个核心命令：

### 1️⃣ 复制应用

```bash
sudo cp -R /Applications/原应用.app /Applications/应用分身.app
```

**作用**：创建应用的完整副本

### 2️⃣ 修改 Bundle ID

```bash
sudo /usr/libexec/PlistBuddy -c "Set :CFBundleIdentifier com.example.app2" \
  /Applications/应用分身.app/Contents/Info.plist
```

**作用**：让系统识别为独立应用

### 3️⃣ 重新签名

```bash
sudo codesign --force --deep --sign - /Applications/应用分身.app
```

**作用**：绕过系统签名验证

### 4️⃣ 启动多开

```bash
open -n /Applications/应用分身.app
```

**作用**：强制打开新实例

---

## 🎨 修改图标（可选）

多开后可以通过修改图标来区分两个实例：

### 快速修改（5 分钟）

1. 右键点击「应用分身.app」→ 显示简介（Command+I）
2. 点击简介窗口左上角的小图标
3. 将准备好的图标拖拽到该位置
4. 输入管理员密码确认

**完成！** 图标立即生效。
详细图标修改方案请查看：[技术原理说明文档](技术原理说明.md#图标自定义方案)

---

## ✅ 验证多开

- 两个应用可以同时运行
- 可以登录不同的账号
- 数据完全隔离
- 互不干扰

---

## 📖 详细文档

需要了解技术原理和更多细节？请查看：

- [技术原理说明](技术原理说明.md) - 完整的技术文档
- [项目 README](../README.md) - 项目说明和法律声明

---

## ❓ 常见问题

### Q: 提示"应用已损坏"怎么办？

重新签名即可：

```bash
sudo codesign --force --deep --sign - /Applications/应用分身.app
```

### Q: 图标带斜杠但能启动？

**这是正常现象！** 系统标记的"可疑应用"提示，不影响使用。

**如需修复**，运行一键修复脚本：

```bash
bash fix-icon-slash.sh
```

或手动修复：

```bash
# 1. 清除隔离属性
xattr -cr /Applications/应用分身.app

# 2. 修复权限
sudo chown -R $USER:staff /Applications/应用分身.app
sudo chmod -R 755 /Applications/应用分身.app

# 3. 重新签名
codesign --force -s - --timestamp=none /Applications/应用分身.app
```

详细说明请查看：[技术原理说明](技术原理说明.md#q15-图标带斜杠但能启动重要)

### Q: 数据会混淆吗？

**不会！** 两个应用的 Bundle ID 不同，数据完全隔离。

### Q: 应用更新后怎么办？

删除多开应用，重新运行脚本即可。

---

**提示**：更多详细问题解答请查看 [技术原理说明](技术原理说明.md#常见问题解答)

*Last Updated: 2026-04-02*
